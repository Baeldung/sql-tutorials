Use University;

SELECT * INTO Active_course
FROM Course
WHERE is_active = 'Yes';

SELECT name
FROM sys.tables;

DROP TABLE IF EXISTS Active_courses;

SELECT name
FROM sys.tables;

DROP TABLE IF EXISTS Active_courses;
